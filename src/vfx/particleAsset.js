export class ParticleAsset {
    /**
     * Preload any assets required for the particle
     * @param {Phaser.Scene} scene 
     */
    preload(scene) {

    }

    /**
     * Create a particle
     * @param {Phaser.Scene} scene 
     * @param {number} x 
     * @param {number} y 
     * @param {number} rotation 
     * @param {number} scale 
     */
    create(scene, x, y, rotation, scale) {

    }

    /**
     * Update the particle if required
     * @param {number} deltaTime 
     */
    update(deltaTime) {

    }
}